
I would also like to navigate primarily through my tags for quicker navigation. Make it like the tags, where the tags are a tag hierarchy specifically for quick retrieval or quick logging if necessary.


I have a lot of links and backlinks that have extremely long names, and a lot of the names are corresponded to the folder notes as well. I don't want those names to look ugly like that. Go ahead and use the Obsidian feature where you can rename a note just by its appearance, but not actually by renaming the note, by putting a bar between the original name of the note or folder note and then adding whatever you want to call the note in that link. So visually in the note, it will actually look like whatever I want to call it. 

The buttons in dark mode are very difficult to distinguish by the way the font between the button and the background. Can we change that as well? Also is there any possibility to make it look like a 3D effect using CSS in light and dark modes? Also the button when I am trying to shift to light mode needs to change to the accent for light mode and I remain blue. 